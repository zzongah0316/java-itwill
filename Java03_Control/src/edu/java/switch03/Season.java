package edu.java.switch03;

// Enum: 열거형 자료 타입(enumeration type). 한정된 값(상수)들만 갖는 데이터 타입.
// (예) 계절(봄, 여름, 가을, 겨울), 식사 시간(아침, 점심, 저녁), 요일(일, 월, ..., 토), ...

public enum Season {
	SPRING, SUMMER, FALL, WINTER; // 상수들은 대문자 사용하는게 관습.

}
